import { EParams } from "./params";

export class Patch {

  constructor(data) {
    this.data = data;
  }

  get param(name) {
    const index = EParams.indexOf(paramName);
  }

  set param(paramName) {

  }

}